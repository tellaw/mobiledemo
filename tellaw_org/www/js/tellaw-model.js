/*
var Post = Backbone.Model.extend({

    defaults: {
        "id":       "",
        "type":     "",
        "slug":     "",
        "url":      "",
        "title":    "",
        "excerpt":  "",
        "content":  ""

    }

});


var Posts = new Backbone.Collection([], {
    model: Post,
    url: 'http://www.tellaw.org?json=1'
});

var posts = new Posts;
posts.fetch();

posts.each(function(posts) {
    console.log(posts.get("title"));
});
*/
console.log ("Model file loaded");